# OS / editor cruft
Thumbs.db
Desktop.ini
.DS_Store
*~

# VS Code (keep the shared workspace file; ignore local state)
.vscode/
*.code-workspace.local

# Claude Code session/history scratch (local, not shared)
.remember/
.claude/

# Prior-project reference DB dump (local-only; mined for content, not shared)
reference_data/

# LLM-maintained design wiki (backed up by OneDrive, not git) + Obsidian vault state
wiki/
.obsidian/

# Temp / build
*.tmp
*.log
node_modules/
dist/
build/
